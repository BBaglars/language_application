const UserService = require('../../services/UserService');
const pool = require('../../db/db');

describe('UserService (Gerçek Veritabanı)', () => {
    // Burada da beforeAll ile gerekli test verilerini ekleyebilirsiniz.
    // afterAll ile de temizleyebilirsiniz.
    // Testlerinizde doğrudan UserService fonksiyonlarını çağırın.
    // Örnek bir test:
    it('dummy', () => { expect(true).toBe(true); });
}); 